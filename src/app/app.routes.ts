import { Routes } from '@angular/router';
import { PresupuestoFormComponent } from './components/presupuesto-form/presupuesto-form.component';

export const routes: Routes = [
    { path: '', component: PresupuestoFormComponent }
];
